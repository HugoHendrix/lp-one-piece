## 🌐 Estrutura Semântica (HTML)

- **Tags semânticas**: use `<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`.
- **Exemplo prático**:
  - `<header>` → logo e menu de navegação.
  - `<main>` → destaque com imagem do Luffy e chamada principal.
  - `<section>` → personagens, história, curiosidades.
  - `<footer>` → links úteis e créditos.

## ♿ Acessibilidade

- **Alt text**: sempre descreva imagens com `alt`.
- **Contraste**: cores fortes (pirata vibes!) mas com contraste adequado.
- **ARIA roles**: em menus e botões interativos.
- **Teclado**: navegação deve funcionar sem mouse.

---

## 🔍 SEO e Performance

- **Meta tags**: título e descrição otimizados.
- **Heading hierarchy**: `<h1>` para o título principal, `<h2>` para subtítulos.
- **Imagens otimizadas**: use formatos leves (WebP).
- **Lazy loading**: carregue imagens apenas quando aparecem na tela.

---

## 🎨 CSS Moderno

- **Flexbox e Grid**: para layout responsivo.
- **Novidades**:
  - `:has()` → seletor relacional moderno.
  - `container queries` → adaptar estilos ao container, não só ao viewport.
  - `clamp()` → tipografia fluida.
- **Pseudo-elementos**: `::before` e `::after` para detalhes visuais (ex: bandeira pirata decorativa).
- **Z-index e position**: para sobreposição de elementos (ex: nav flutuante).

---

## ⚡ JavaScript Interações

- **Menu hamburguer**: abrir/fechar em mobile.
- **Scroll suave**: navegação entre seções.
- **Modal**: exibir curiosidades ou vídeos de One Piece.
- **Formulário de inscrição**: validar email para “entrar na tripulação”.

---